# Data Model

No changes to the underlying data structure are required for this visual redesign. 

The existing data files (`src/data/profile.js`, `src/data/projects.js`, `src/data/experience.js`, `src/data/technologies.js`, and `src/data/ui.js`) will be reused. 

*Note: As per the user's request to make the design minimalist and remove verbose information, the `blurb` fields inside `technologies.js` will simply be ignored/omitted by the redesigned `Skills` component during rendering.*
